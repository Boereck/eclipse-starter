language: rust
rust:
  - stable

os:
  - linux
  - osx
  - windows


before_install:
  - if [ "$TRAVIS_OS_NAME" = "windows" ]; then choco install -y windows-sdk-10 ; fi

script:
  cargo build --release